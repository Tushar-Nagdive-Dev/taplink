export const environment = {
  production: true,
  environmentName: 'PROD',
  // Relative URL: since Spring Boot serves the app on the same domain, leave this empty
  // so requests automatically go to "/api/v1/..." on whatever host it runs on!
  apiBaseUrl: ''
};
